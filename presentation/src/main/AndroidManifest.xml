<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.workFlowy.MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/workfolwy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/workfolwy_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkFlowy"
        tools:targetApi="31">
        <activity
            android:name="com.example.workFlowy.screen.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WorkFlowy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>>
    </application>

</manifest>